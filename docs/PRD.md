# PRD — 디버그 119 (바이브 코딩 막힘 가이드)

> 문서: PRD.md · 상태: 초안(1차 개발 반영) · 열린 질문 [?] 있음 · 마지막 갱신: 금
> 이 문서를 읽는 에이전트에게: 여기 없는 것은 지어내지 말고 질문으로 돌려라. 「안 만드는 것」에 있는 것은 만들지 않는다.

> 강사 결정(금): 기획 문서를 촘촘히 쓰지 않고 01 · 02 만으로 1차 개발을 한다. 수정 범위는 강사가 개발 결과를 보고 말한다. 이 문서는 1차 개발에서 만든 것을 그대로 적은 것이다.

## 1. 만드는 것 (1차)

| 기능 | 하는 것 | 주소 |
|---|---|---|
| 증상별 해결 가이드 | 홈에서 자주 생기는 유형 8개 중 하나를 고르면 그 유형의 증상만 보인다(강사 결정: 한 페이지에 다 넣지 않는다). 증상 → 원인 → 먼저 확인할 것 → AI 도구에 붙여넣을 프롬프트(복사 버튼). 내용은 `src/content/guides.ts` 를 강사가 직접 고친다 | `/` (유형 고르기) · `/guide/[유형]` |
| 점검 체크리스트 | 파트 나누기 전 · AI에게 시키기 전 · DB 붙이기 전 · 합치기 전 · 배포 전. `src/content/checklists.ts` | `/checklist` |
| 질문 게시판 | 증상 한 줄 + 무엇을 하다가 · 언제부터 · 어떻게 했나 · 예상 값 · 실제 값 (01 강사 결정). 강사와 학생이 답한다. 강사 답은 색으로 구분 | `/board` · `/board/new` · `/board/[id]` |
| 로그인 | 수강생만(01). 강사 계정과 학생 공용 계정은 강사가 만든다 — `npm run user:add -- 이름 비밀번호 [instructor\|shared]` | `/login` |
| 내 계정 만들기 | 학생은 공용 계정으로 처음 들어오면 바로 닉네임(로그인 ID) · 이름(강사만 봄) · 비밀번호를 정해 자기 계정을 만든다 (강사 결정) | `/setup` |

## 2. 안 만드는 것

| 후보 | 출처 | 이유 |
|---|---|---|
| 에러 붙여넣기 AI 진단기 | 01 형태 선택 | 비용 — 자주 생기는 유형을 미리 정리해 참고하게 하는 것으로 대신한다 |
| 학생 프로젝트(GitHub · Vercel · Neon)에 직접 접속 | 01 [제안] | 세 형태 모두 접속이 필요 없다 |
| 회원 가입 | 01 "수강생만" | 계정은 강사가 만든다 |
| 가이드 · 체크리스트 편집 화면 | 1차 개발 | 강사가 파일을 고치는 것으로 충분하다 — 필요해지면 그때 |

## 3. 저장 항목

| 항목 | 칸 |
|---|---|
| 계정 | 닉네임(유일 · 로그인 ID) · 이름(학생만 · 강사만 봄) · 비밀번호(해시) · 역할(강사/학생/학생 공용) · 만든 시각 |
| 질문 | 제목 · 무엇을 하다가 · 언제부터 · 어떻게 했나 · 예상 값 · 실제 값 · 쓴 사람 · 쓴 시각 |
| 답 | 어느 질문 · 쓴 사람 · 내용 · 쓴 시각 |

가이드 · 체크리스트 내용은 DB 가 아니라 파일(`src/content/`)에 있다.

## 4. 열린 질문

- 01 의 [?] 4개 · [제안] 4개 — 아직 채택/거절 전
- 02 업무 분석 초안 전체 — 강사 검토 전
- 사이트 이름 — "디버그 119" (강사가 에이전트에게 맡김 · 막히면 부르는 곳이라는 뜻)
- 가이드 항목 17개의 내용 — 초안 작성자가 쓴 것. 강사가 실제 수업 관찰로 고친다
