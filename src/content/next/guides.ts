/**
 * 증상별 해결 가이드 — M3 TypeScript · Next.js 프론트엔드. 강사가 이 파일을 직접 고친다.
 * 모양은 vibe/guides.ts 와 같다. CATEGORIES 에 유형을, GUIDES 에 증상을 넣으면 화면에 바로 나온다.
 * 비어 있으면 화면에 "아직 준비 중" 이 보인다.
 */
import type { Category, Guide } from "../types";

export const CATEGORIES: Category[] = [];

export const GUIDES: Guide[] = [];
