/**
 * 정보 공유 — M2 Java · Spring 백엔드. 강사가 이 파일을 직접 고친다.
 * 묶음마다 제목 · 본문(줄바꿈 그대로) · 링크(선택). 비어 있으면 화면에 "아직 준비 중" 이 보인다.
 */
import type { TipSection } from "../types";

export const TIPS: TipSection[] = [];
